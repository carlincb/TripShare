<!-- Loop over each object in the blog array -->
{{#each blogs as |blog| }}
<div class="row mb-4 blog justify-center" blog-id={{blog.id}}>
  <div class="col-md-6">
    <h2>
      <!-- Call helper function with triple brackets to render the emoji span as HTML -->
      
      <a href="/blog/{{blog.id}}">{{blog.title}}</a>
    </h2>  
    {{#if blog.image}}
    <div class="imagesize">
    <img src="upload/{{blog.image}}">
    </div>
    {{/if}}
    <p>Posted by {{blog.user.name}} on {{format_date blog.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{blog.content}}
    </p>
  </div>
    <div class="col-md-6 justify-center blog-block">
      <textarea rows="5" cols="20"></textarea> 
      <div class="col">
      <button class="comment-btn col">ADD COMMENT</button>
      </div>
      <div class= "upvote-container row justify-center">
      <button class="like-btn" data-blog-id={{blog.id}}>
        <i class="fa fa-thumbs-up"></i>
      </button>
      <p class="likecount">0</p>
      <button class="dislike-btn" data-blog-id={{blog.id}}>
        <i class="fa fa-thumbs-down"></i>
      </button>
      <p class="dislikecount">0</p>
      </div>
    </div>
</div>
{{/each}}
